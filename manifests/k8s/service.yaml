apiVersion: v1
kind: Service
metadata:
  name: gitops-springboot-service
spec:
  selector:
    app: gitops-springboot
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
